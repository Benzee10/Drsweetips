# Football Accumulator App

## How to Run
1. Create `.env.local` file with Firebase credentials (already included)
2. Run:
```bash
npm install
npm run dev
```
3. Open `http://localhost:3000` to view the app
